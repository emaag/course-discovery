# Dependencies
vendor/
node_modules/

# Environment
.env
.env.*

# Docker volumes (bind-mounted named volumes are managed by Docker, not git)
wordpress_data/
db_data/

# SQL dumps for local import (may contain data, kept out of version control)
db/*.sql
db/*.sql.gz

# OS / editor cruft
.DS_Store
Thumbs.db
.idea/
.vscode/
